let home=(csv example_data/home.csv name=$type.text country=$type.text)
let age=(csv example_data/age.csv name=$type.text age=$type.integer)

struct l=$home r=$age | join %l.name %r.name | sort %name
